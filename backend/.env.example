# TRINETRA backend
#
# Phases 1-2 (environment + tools) are standard library only.
# Phase 3 (autonomous agent) needs the Gemini SDK.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash
GEMINI_FALLBACK_MODEL=gemini-3.1-flash-lite
